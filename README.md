# Machine-Learning
Binary classifier to predict Titanic passenger survival using a Decision Tree. Includes EDA, sklearn preprocessing pipeline (imputation + encoding), depth tuning via validation set, and final evaluation on test set. Achieves
